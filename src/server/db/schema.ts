/**
 * Drizzle ORM スキーマ定義。
 *
 * Phase 1(プロジェクト初期化)時点ではテーブルは未定義。
 * ドメインモデル(Product, MarketIntelligence, Opportunity, Experiment, …)は
 * docs/architecture.md の設計に基づき、Phase 2以降で各フェーズが必要とする
 * テーブルをこのファイルに追加していく。
 *
 * 追加時は `pnpm db:generate` でマイグレーションSQLを生成し、
 * 内容を目視確認してからコミットすること。
 */

export {};
