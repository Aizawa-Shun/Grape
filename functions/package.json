{
  "name": "grape-loop",
  "private": true,
  "description": "Scheduled trigger for Grape's weekly loop (POST /api/cron/tick on the App Hosting backend).",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^14.5.0",
    "firebase-functions": "^7.4.0"
  }
}
